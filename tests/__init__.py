# -*- coding: utf-8 -*-
"""The sublevel_rules test package.

The tests are written with the standard library ``unittest`` so they also run in
offline environments, and they stay compatible with ``pytest``
(``python -m pytest tests``).
"""
