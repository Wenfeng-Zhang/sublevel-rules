# Consistent line endings: always LF inside the repository, so core.autocrlf on
# Windows cannot turn them into CRLF.
* text=auto eol=lf

# Binary files get no line-ending conversion
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.zip binary
